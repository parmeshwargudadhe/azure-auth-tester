# azure-auth-tester
This Python script safely tests multiple Azure authentication methods — Managed Identity, Azure CLI, Visual Studio Code and DefaultAzureCredential — for access to Azure Log Analytics. It does not execute any queries; it only verifies that a valid token can be acquired.
